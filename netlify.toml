[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes (e.g. /join/<token>, /privacy) resolve to index.html.
# public/_redirects also covers this; kept here so config lives in one place too.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
